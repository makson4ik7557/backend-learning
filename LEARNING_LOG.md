10.06.2026
Збудував: фільтрація GET /markets за статусом через query (?status=)
Зламалось: переплутав гілки — фільтрував коли параметра нема, віддавав усе коли є; повертало навпаки
Зрозумів: ?status після ? — це req.query, не новий маршрут; голий і з query йдуть в один хендлер, фільтр — необов'язкова гілка.

11.06.2026
Збудував: типізована модель Market (interface + union для status) і дві типізовані функції find/filter на TS
Зламалось: плутав «масив vs один об'єкт» — методи масиву на типі Market без []; | undefined там, де filter завжди вертає масив
Зрозумів: тип має відповідати тому, що реально летить у функцію і вертається з неї; TS ловить невідповідності в компайл-таймі, до запуску.

12.06.2026
- Збудував: виніс union статусу в окремий тип (type MarketStatus = "unresolved" | "resolved"), підставив у interface Market і в параметр функції — прибрав потрійний дубль
- Зламалось: нічого критичного; зрозумів конвенцію — типи називають з великої літери (MarketStatus, не marketStatus)
- Зрозумів: type беруть для union (interface так не вміє); іменований тип = одне джерело правди, новий статус додається в одному місці

2026-06-13
- Збудував: переписав сервер з app.js на app.ts — типізовані хендлери (Request/Response), спільний тип Market через export/import, перехід require→import і проєкт на ES-модулі ("type":"module"); прибрав мертві сервери (server.js, express-server.js, старий app.js)
- Зламалось: ланцюг конфіг-помилок — TS1295 (import у CommonJS-файлі, лікується "type":"module"), TS1484 (Request/Response це типи → потрібен import type), TS2835 (у ESM відносний імпорт вимагає розширення → "./markets.js")
- Зрозумів: межа захисту TS — params.id це string, бо форма видна в маршруті наперед; req.body це any, бо це дані від клієнта невідомої форми, а TS перевіряє лише компайл-тайм, тіло ж приходить у рантаймі вже без типів → тип на вході ≠ перевірені дані, звідси далі потрібна валідація

2026-06-14
Додав валідацію присутності й типів на POST і PATCH /markets (typeof-чеки, 400 при невалідному body).
Зловив: TS2345 — string не присвоюється marketStatus (union вужчий за string), бо валідація перевіряє тип, але не дозволене значення.
Зрозумів: typeof-чек ловить тип, але не value; status="asdfgh" пройшов би. Завтра — нормальна перевірка через .includes замість as-каста.

2026-06-15
Закрив борг з минулого дня: прибрав as-касти, зробив type guard isMarketStatus (curStatus is marketStatus) з .includes; виніс валідацію статусу + тип + масив у markets.ts, розділив import type / import value в app.ts.
Зловив: TS1361 — значення не можна тягнути через import type (стирається в рантаймі); звуження змінної не протягується в новостворений об'єкт; allowedStatuses-масив джерело, marketStatus виведений через typeof[number] + as const.
Зрозумів: межа тип/значення скрізь одна (as const, type guard, import) — тип це компайл-тайм і стирається, значення живе в рантаймі. Код живе там, де дані: усе про статус → markets.ts, app.ts тільки HTTP.

2026-06-16
Виніс валідацію body з POST/PATCH у middleware (validateCreate / validateUpdate), хендлери схудли до бізнес-логіки. Налаштував запуск через tsx (npm run dev з watch) замість зламаного node app.ts.
Зловив: node app.ts падає на TS+ESM; middleware це (req,res,next) — перевірив → 400+return або next(); req.market з типізацією свідомо не брав, лишив простий пошук у хендлері.
Зрозумів: middleware не прибирає if, а прибирає дубль і розвантажує хендлер; рукописна валідація = проміжна форма, далі zod; одна-чи-дві middleware вирішує кількість випадків, не «щоб не змішувати».
